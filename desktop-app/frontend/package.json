{
  "name": "frontend",
  "private": true,
  "version": "0.0.0",
  "scripts": {
    "dev": "vite",
    "build": "vite build && node scripts/keep-dist-anchor.mjs",
    "preview": "vite preview",
    "test": "vitest run",
    "lint:css": "stylelint \"src/**/*.css\"",
    "lint:js": "eslint src scripts",
    "lint": "npm run lint:js && npm run lint:css"
  },
  "devDependencies": {
    "@eslint/js": "^10.0.1",
    "eslint": "^10.11.0",
    "globals": "^17.12.0",
    "stylelint": "^17.15.0",
    "stylelint-config-standard": "^40.0.0",
    "stylelint-value-no-unknown-custom-properties": "^6.1.1",
    "vite": "^8.3.0",
    "vitest": "5.0.1"
  }
}
